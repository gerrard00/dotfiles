set lazyredraw " to avoid scrolling problems in mac

" at home I only use eslint
let g:syntastic_javascript_checkers = ['eslint', 'jscs']

"enable mouse scrolling, will help during demos
:set mouse=a
